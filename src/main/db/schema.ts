/**
 * Drizzle schema for LedgerPage.
 *
 * Slice 3 (SQLite and Migration Bootstrap) intentionally defines no
 * business tables. This file exists so drizzle-kit has a schema to diff
 * against; the migration-tracking table itself is created and managed by
 * Drizzle's migrator, not defined here.
 */
export {}
