// Slice 1 stub only.
//
// This file intentionally exposes no APIs. Its only job right now is to
// prove the main/preload/renderer build pipeline works end to end. Slice 2
// introduces the hardened contextBridge surface (small, typed, allow-listed)
// per the approved Electron security requirements. Do not add
// contextBridge.exposeInMainWorld calls here until that slice.
